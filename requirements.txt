# ==========================================
# AgentLumy — Dépendances d'EXÉCUTION (backend)
# ==========================================
# Ce fichier ne contient QUE ce dont le backend a besoin pour tourner.
# C'est lui qui est installé dans l'image Docker : chaque paquet en trop
# alourdit l'image, donc le démarrage à froid de Cloud Run, donc la latence
# du premier appel — l'objectif de l'étape 12 est < 800 ms.
#
#   Développement (tests, lint, scripts) : requirements-dev.txt
#   Dashboard Streamlit                  : requirements-dashboard.txt
#
# Vérifié par grep des imports de app/ : le backend n'importe rien d'autre.

# ===== Framework web =====
# fastapi >= 0.139 requis : streamlit 1.58 impose starlette 1.x, incompatible
# avec fastapi 0.115. On garde le même pin partout pour éviter deux résolutions.
fastapi==0.139.2
starlette>=1.3.0
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# ===== Configuration =====
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ===== Client HTTP (Cal.com, Vapi) =====
httpx==0.27.2

# ===== Base de données =====
supabase==2.10.0
postgrest==0.18.0

# ===== Téléphonie et email =====
twilio==9.3.7
resend==2.5.1

# ===== Utilitaires =====
phonenumbers==8.13.50

# Requis pour ZoneInfo("Europe/Paris") : Windows n'a pas de base tz système,
# et les images python:slim non plus.
tzdata==2024.1
